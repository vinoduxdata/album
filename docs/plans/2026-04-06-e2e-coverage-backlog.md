# E2E API Coverage Backlog

**Companion to:** [`2026-04-06-e2e-api-coverage-research.md`](./2026-04-06-e2e-api-coverage-research.md)

This is the working backlog for closing the e2e API test coverage gaps. Each row is one PR, scoped small enough to review in under 30 minutes. Tasks are listed roughly in dependency order; once `T02` (helpers) lands, most of the remaining tasks can be picked up in any order.

## How to use this doc

- **Pick a task off the list.** Read its blurb here and the relevant section of the research doc.
- **If the task has a design doc linked**, read that for the architectural decisions.
- **If it doesn't**, the blurb here IS the spec — the test cases are derivable from the actor matrix in §3 of the research doc plus the per-file lists in §4.
- **When you ship**, tick the checkbox and add the PR number inline: `- [x] T05 — … (#264)`.
- **If you discover decisions** that needed making (helper API change, new actor, fixture surprise), add a new design doc at `docs/plans/2026-04-06-e2e-T<NN>-<slug>-design.md` and link it from the row.

## Conventions

- **Task ID**: `T<NN>` — stable, used in commit messages and PR titles. Format: `test(e2e): T05 timeline /buckets access matrix`.
- **Size hint**: number of new tests, not lines. ~5-15 per task is the target band.
- **Actor matrix**: which subset of §3.1 the task exercises. "Std space" = `anon, regularA, spaceOwner, spaceEditor, spaceViewer, spaceNonMember`.
- **Blocked by**: hard prerequisite. Anything not listed can run in parallel.

---

## Phase 0 — Cleanup & helpers (must land before everything else)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Size                                                     | Blocked by | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ---------- | ------ |
| T01 | **Cleanup**: audit `expect.poll` vs `waitForQueueFinish` for non-admin specs (memory: `feedback_e2e_admin_only_queues`). _Audit result: zero violations — all 38 callers use `admin.accessToken`. Rule currently held everywhere._ ⚠️ The original task also planned to move `e2e/src/api/specs/duplicate.e2e-spec.ts` into the discovered glob, but the file is **upstream-broken** (PR #25316 added it in the OLD location after PR #25856 had moved the e2e layout six weeks earlier; the never-running spec went stale and 18 of 21 tests fail against the current API shape). The move was reverted to avoid maintaining a fork-local fix that conflicts every rebase. **See T-cleanup-01 below.** | small                                                    | —          | [x]    |
| T02 | **Helpers**: add `Actor`, `ActorId`, `SpaceContext` types, `buildSpaceContext`, `forEachActor` in new `e2e/src/actors.ts` (composes existing `utils.ts` helpers). Extend `utils.createSpacePerson` to add the `shared_space_person_face` junction insert, `type` param, and `{globalPersonId, spacePersonId, faceId}` return shape. Includes 4 smoke tests in `_helpers.e2e-spec.ts` (auth threading + anon split + helper extension + role assignment). **Has design doc:** [T02-helpers-design.md](./2026-04-06-e2e-T02-helpers-design.md)                                                                                                                                                            | 4 smoke tests, ~120 lines actors.ts + ~30 lines utils.ts | T01        | [x]    |

---

## Phase 1 — Highest-leverage gaps (P0 from research doc)

### Timeline (`timeline.controller`, fully uncovered)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                   | Size                                                | Blocked by                  | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | --------------------------- | ------ |
| T03 | **`timeline.e2e-spec.ts` — access matrix**. `GET /timeline/buckets` and `/bucket` against the std space actor matrix. Owner sees own; non-member **400** (timeline uses `requireAccess`, not `requireMembership`); non-owner members actually see space content. **Has design doc:** [T03-timeline-access-design.md](./2026-04-06-e2e-T03-timeline-access-design.md)                                   | 9 tests (design's tests 3+6 merged into one matrix) | T02                         | [x]    |
| T04 | **Timeline `withSharedSpaces` and `withPartners` semantics**. Toggle `showInTimeline` per space, verify the UNION includes/excludes correctly. Adds `partner` actor + `addPartner` helper to T02's surface (auto-enables `inTimeline` since the column defaults to false). Library-linked space assets deferred to T17.                                                                                | 5 tests                                             | T02 (recommended after T03) | [x]    |
| T05 | **Timeline visibility filters**. `visibility=Timeline\|Archive\|Hidden`, trash exclusion, **non-obvious default = Timeline + Archive** (not just Timeline). Plus the visibility-undefined-throws-400 invariant pin folded in from the T04 review NIT.                                                                                                                                                  | 7 tests                                             | T03                         | [x]    |
| T06 | **Timeline filter passthrough with `spaceId`**. `spacePersonId` is a distinct DTO param from `personId` — passing a global `personId` on a space query is the PR #260 bug shape (silently empty or cross-pollutes). Plus `tagIds` cross-filtered with `spaceId`. EXIF filters (country/make/rating) deferred — they require fixture images with metadata which the test stack uses generated PNGs for. | 5 tests                                             | T03                         | [x]    |

### Faces (`face.controller`, fully uncovered)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                      | Size     | Blocked by | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ |
| T07 | **`face.e2e-spec.ts` — CRUD access matrix**. Create / list / reassign / delete with owner + non-owner. Cross-owner reassign rejected. Pinned the non-obvious API shape: PUT `/faces/:personId` with body `{id: faceId}` (path is target person, body is face). Extended `utils.createFace` to return the face id.                                                                                                         | 10 tests | T02        | [x]    |
| T08 | **Face deletion side effects**. Soft-delete excludes from GET via `deletedAt` filter, hard-delete same. Person row preserved when its only face is removed. Person `getStatistics` decreases on both delete paths. Re-attaching the same (assetId, personId) after a soft-delete inserts a new row. _Below-minFaces is a space-person concern (PR #139) — moved to T10/T11. Dedup queue jobId dedup (PR #292) is in T14._ | 6 tests  | T02, T07   | [x]    |

### Shared space — people sub-tree (`shared-space.e2e-spec.ts` extension)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                    | Size     | Blocked by | Status |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ |
| T09 | **`GET /shared-spaces/:id/people` — listing**. Std space access matrix. Excludes hidden by default + `withHidden=true` includes them. `named` filter. `petsEnabled` toggle. `limit`/`offset` pagination. Empty `thumbnailPath` excludes (the listing's hard requirement). **Has design doc:** [T09-space-people-listing-design.md](./2026-04-06-e2e-T09-space-people-listing-design.md) | 11 tests | T02        | [x]    |
| T10 | **`GET /shared-spaces/:id/people/:personId` + thumbnail + assets**. Read-only sub-endpoints. Resolved the T09 open hypothesis: hidden filter is listing-only (✓), pet filter applies to BOTH listing AND single-fetch (asymmetry). Pinned 400-not-404 for missing personId. Thumbnail member-success returns 500 because the fixture thumbnailPath is fictional — known footgun.        | 9 tests  | T02, T09   | [x]    |
| T11 | **`PUT/DELETE /shared-spaces/:id/people/:personId`**. Rename, mark hidden (verifies hidden-after-PUT is excluded from listing but still fetchable directly — pairs with T10's listing-only invariant), delete. Editor + Owner can; Viewer 403; non-member 403; anon 401. Delete preserves the underlying global person row (no cascade). Missing personId 400.                          | 7 tests  | T02, T09   | [x]    |
| T12 | **`POST /shared-spaces/:id/people/:personId/merge`**. Editor required. Merging reassigns the source's junction rows to the target, deletes the source row, and recounts the target's denormalised faceCount/assetCount. Cannot merge a person into themselves. Cannot merge across types (person ↔ pet rejected). 400 for missing target/source.                                        | 6 tests  | T02, T09   | [x]    |
| T13 | **`PUT/DELETE /shared-spaces/:id/people/:personId/alias`**. Aliases are PER-USER (not visible to all members — the original design row was wrong). Viewers CAN set their own aliases (alias requires `requireMembership` only, NOT `requireRole(Editor)`). Alias does NOT modify global `person.name`. DELETE is idempotent on missing personId; PUT validates and returns 400.         | 5 tests  | T02, T09   | [x]    |
| T14 | **`POST /shared-spaces/:id/people/deduplicate`**. Owner-only (NOT just Editor — distinct from PUT/DELETE/merge). Queues `SharedSpacePersonDedup` on the FacialRecognition queue with jobId `space-dedup-${spaceId}`. PR #292 jobId dedup pinned via pause+verify: two calls in quick succession enqueue exactly one job.                                                                | 4 tests  | T02, T09   | [x]    |

### Shared space — libraries sub-tree

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                     | Size    | Blocked by         | Status |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------ | ------ |
| T15 | **`PUT /shared-spaces/:id/libraries`**. Two-step gate: admin gate fires FIRST (`if (!auth.user.isAdmin) → 403 'Only admins'`), then `requireRole(Editor)`. Non-admin owner of space cannot. Admin who's a space Editor CAN. Duplicate (spaceId, libraryId) is **idempotent at 204** (not 409) — `addLibrary` returns null on duplicate without error. Missing library returns 400.                                                       | 7 tests | T02                | [x]    |
| T16 | **`DELETE /shared-spaces/:id/libraries/:libraryId`**. Same two-step gate as T15 (admin first, then `requireRole(Editor)`). Idempotent on missing link AND on bogus libraryId → **204**, NOT 404 (the original design row was wrong). Repository call is a plain DELETE with no row-existence check.                                                                                                                                      | 5 tests | T02, T15           | [x]    |
| T17 | **Library link side effects**. After link, library assets visible to space members via `/timeline/bucket?spaceId=`. After unlink, hidden. Soft-deleted assets hidden via `deletedAt` filter. **Offline assets are NOT hidden** (asset.repository.ts:835-849 has no isOffline filter on the timeline path — surprising finding). Library delete cascades to `shared_space_library` ASYNC via the LibraryDelete job (soft-delete + queue). | 6 tests | T02, T15, T03, T05 | [x]    |

---

## Phase 2 — Fork-only, smaller surfaces (P1 from research doc)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                     | Size     | Blocked by | Status  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ------- |
| T18 | **`gallery-map.e2e-spec.ts` — access matrix + filters**. Auth, owner happy path, country/city/isFavorite/takenAfter/takenBefore filters, rating/type validation, archived hidden via hardcoded `visibility=Timeline`, cross-user isolation.                                                                                                                                                                              | 12 tests | T02        | [x]     |
| T19 | **`gallery-map` — space scoping**. spaceId access matrix (non-member 400 via requireAccess), member sees space asset, owner with spaceId is space-scoped not full library, missing spaceId 400, filters compose with spaceId, **personIds re-routes to spacePersonIds when spaceId is set**. _Hidden-persons exclusion (PR #202) deferred to a follow-up — would need a shared_space_person fixture with isHidden=true._ | 7 tests  | T02, T18   | [x]     |
| T20 | ~~**`map.e2e-spec.ts` — space scoping extension**~~ **N/A — `/map/markers` has no `spaceId` support.** Verified at `server/src/dtos/map.dto.ts:29-47` (no `spaceId` field) and `server/src/services/map.service.ts:9-27` (no spaceId branch). Space-scoped map queries are entirely on the fork-only `/gallery/map/markers` endpoint covered by T18+T19.                                                                 | 0 tests  | T02        | [x] N/A |
| T21 | **`view.e2e-spec.ts`**. `GET /view/folder/unique-paths`, `GET /view/folder`. Both endpoints are strictly owner-scoped (`view.service.ts:8-15` — `auth.user.id` only, no partner/space/library JOINs). Cross-user isolation pinned. Missing `path` query param returns **500** (no validation in controller — small upstream bug worth filing).                                                                           | 9 tests  | T02        | [x]     |
| T22 | **`workflow.e2e-spec.ts`**. CRUD access matrix. Auth + create (empty filters/actions OK, bad trigger 400, bad pluginFilterId 400, empty name 400) + getAll owner-scoped + GET/PUT/DELETE cross-user 400 (bulk-access pattern) + empty PUT body 400 + delete + cross-user delete 400.                                                                                                                                     | 15 tests | T02        | [x]     |

---

## Phase 3 — Asset extensions (P2 from research doc)

These split `asset.e2e-spec.ts` into separate small specs to avoid 2k-line files.

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Size     | Blocked by | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ |
| T23 | **`asset-metadata.e2e-spec.ts`** (new file). `GET/PUT /assets/:id/metadata` + `/:key`, bulk `PUT/DELETE /assets/metadata`. Owner upsert + read + delete + bulk + cross-owner 400 (bulk-access pattern). _Space-member access deferred — would need a space fixture in the asset metadata test, more involved than the T23 scope._                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 13 tests | T02        | [x]    |
| T24 | **`asset-ocr.e2e-spec.ts`** (new file). `GET /assets/:id/ocr`. Auth + owner happy path (empty array for unprocessed asset, NOT 404), non-owner 400 (bulk-access pattern), admin-cross-user 400 (no admin override), missing asset 400, malformed UUID 400. _Generated test PNGs have no OCR data, so the "returns text for processed asset" path is deferred — would need a fixture with real OCR-eligible content and a wait on the OCR queue._                                                                                                                                                                                                                                                                                                                                                                                | 6 tests  | T02        | [x]    |
| T25 | **`asset-edits.e2e-spec.ts`** (new file). `GET/PUT/DELETE /assets/:id/edits` for non-trim actions (existing `video-trim.e2e-spec.ts` covers trim only). Full access matrix per verb (auth/non-owner/admin-cross-user/missing/malformed). PUT crop/rotate/mirror happy paths + dual-axis mirror positive case (UniqueEditActions asymmetry). Crop-must-be-first ordering rule (message-pinned). Crop-bounds violation (message-pinned to defend against dim-extraction race). Non-axis-aligned rotation (DTO). ArrayMinSize empty edits. UniqueEditActions (two rotates). DELETE access + round-trip. _Uses `makeRandomImage()` 1x1 PNG so the only valid crop is x=0,y=0,w=1,h=1; bounds error tested with w=2,h=2. Image-format rejections (live-photo, panorama, .gif, .svg) deferred — would need format-specific fixtures._ | 25 tests | T02        | [x]    |
| T26 | **`asset-copy.e2e-spec.ts`** (new file). `PUT /assets/copy`. _Backlog description was wrong: this endpoint copies association metadata (favorite/albums/sidecar/sharedLinks/stack) FROM source TO target — no new asset is created and there's no quota change._ Pinned: auth, sourceId===targetId 400, missing source 400, missing target 400, malformed UUID 400, non-owner-of-source 400, non-owner-of-target 400, admin-cross-user 400 (no admin override), favorite propagation happy path, favorite=false opt-out, album propagation happy path, albums=false opt-out. _Stack and sharedLinks copy deferred — would need a stack fixture or shared-link fixture, more setup than T26 justifies._                                                                                                                          | 12 tests | T02        | [x]    |
| T27 | **`asset-replace-jobs-bulk.e2e-spec.ts`** (new file, NOT folded into `asset.e2e-spec.ts`). `PUT /assets/:id/original` + `POST /assets/jobs` + `POST /assets/bulk-upload-check` — three small surfaces in one spec. Replace: auth, owner happy path (status REPLACED, returned id ≠ original), non-owner 400, missing 400, malformed 400. Jobs: auth, owner queues regenerate-thumbnail (204), non-owner 400, invalid job name 400, malformed UUID in list 400. BulkUploadCheck: auth, unknown checksum → ACCEPT, owner's own checksum → REJECT/DUPLICATE with the asset id, cross-user isolation (other user's checksum → ACCEPT, no leak).                                                                                                                                                                                     | 14 tests | T02        | [x]    |
| T28 | **`asset-video-device.e2e-spec.ts`** (new file, NOT folded). `GET /assets/:id/video/playback` + `GET /assets/device/:deviceId`. Video playback: auth, owner streams real video (200, content-type video/\_, non-zero bytes), non-owner 400 (bulk-access), image asset 404 (getForVideo filters on type=Video), missing 400, malformed 400. Device endpoint: auth, owner gets deviceAssetIds from a known deviceId, unknown deviceId → empty array, cross-user isolation (other user with the SAME deviceId sees nothing because the service scopes by auth.user.id). Uses `normal.mp4` fixture shared with `video-trim.e2e-spec.ts`.                                                                                                                                                                                            | 10 tests | T02        | [x]    |

---

## Phase 4 — Config-driven features (P2 from research doc)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Size     | Blocked by | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ |
| T29 | **`classification.e2e-spec.ts` — effects** (extends existing spec). DTO validation: duplicate names (UniqueNames), invalid action (IsIn), similarity bounds (Min 0 / Max 1), empty prompts (ArrayMinSize). Round-trip: custom categories preserved across PUT/GET, empty categories with enabled=true valid. _Smart re-scan side effects (removing category / bumping similarity → strips Auto/\* tags) deferred at e2e layer — the cross-worker socket.io→Redis pub/sub→microservices propagation chain is reliable in isolation but ~50% flaky when run alongside other system-config-touching specs. Side-effect logic IS covered by unit tests at classification.service.spec.ts onConfigUpdate. Scan-effect ("category tag appears after scan") also deferred — needs ML server (disabled in e2e)._ | 9 tests  | T02        | [x]    |
| T30 | **`system-config.e2e-spec.ts` — full coverage** (extends existing spec). Access matrix on GET / GET defaults / PUT (auth + admin gate). GET /defaults shape parity with GET. GET /storage-template-options. PUT round-trip + invalid storage template (kept from existing). PUT validation: machineLearning.clip.maxDistance bounds (Min 0, Max 2) — PR #294, machineLearning.urls required when enabled. PUT round-trip on trash.days and theme.customCss. _IMMICH_CONFIG_FILE lock branch (PR #297) NOT pinned — requires restarting the e2e stack with the env var set; covered by unit tests at system-config.service.spec.ts. No `sharedSpace` config field exists in current schema (backlog assumption was wrong)._                                                                               | 17 tests | T02        | [x]    |

---

## Phase 5 — Smaller fillers (P2/P3 from research doc)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Size     | Blocked by | Status |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------ |
| T31 | **`notification.e2e-spec.ts`** (new file). `/notifications` GET/PUT/DELETE (single + bulk) + `/admin/notifications` POST/test-email/templates. Auth + admin gate matrix per verb. Cross-user isolation on list and PUT. Bulk PUT/DELETE round-trips. _Pins an UPSTREAM BUG (filed as T-cleanup-02): notification.repository.ts:70 uses `WHERE deletedAt IS NOT NULL` in `get(id)` so `GET /notifications/:id` ALWAYS returns 400 for active notifications. Test asserts the broken behavior with a bug-marker comment so a future fix triggers a deliberate update._                                                                                                              | 18 tests | T02        | [x]    |
| T32 | **`plugin.e2e-spec.ts`** (new file). Read-only coverage for `/plugins`, `/plugins/triggers`, `/plugins/:id`. Auth + non-admin pass (no admin gate). Asserts the bundled `immich-core` plugin loaded with all 3 filters (filterFileName/filterFileType/filterPerson) and all 3 actions (actionArchive/actionFavorite/actionAddToAlbum). Pins AssetCreate as the only registered trigger. Single-fetch by id + 400 for missing/malformed UUID. _Backlog said "classification, pet-detection plugins" — those are services in `src/services/`, NOT extism plugins. The actual fork-bundled plugin is `plugins/manifest.json` (immich-core); the spec docstring corrects the record._ | 9 tests  | T02        | [x]    |
| T33 | **`search-smart-sort.e2e-spec.ts`** (new file, NOT folded into the 1100-line search spec). PR #254 two-phase CTE smart-search ordering. Pins auth + DTO validation + the 'Smart search is not enabled' gate ordering (spacePersonIds-without-spaceId fires before the gate; missing query/queryAssetId fires after it). _Sort logic itself is NOT pinned at e2e — the e2e Docker stack disables ML, which gates /search/smart unconditionally. Filed as T-cleanup-03: an embedding-seeding helper would unlock real asc/desc/relevance assertions._                                                                                                                               | 6 tests  | T02        | [x]    |
| T34 | **`tag-suggestions.e2e-spec.ts`** (new file). `GET /search/suggestions/tags` — PR #230's `withSharedSpaces` flag and the spaceId scoping branch. Auth, default scope returns caller's own tags only (cross-user isolation, both directions), spaceId scope surfaces tags from another user's asset added to the space, withSharedSpaces=true expands listings via the joined-spaces union, non-member-of-spaceId 400 (requireAccess), spaceId+withSharedSpaces both → 400 'Cannot use both'.                                                                                                                                                                                      | 7 tests  | T02        | [x]    |
| T35 | **`person-search-reassign.e2e-spec.ts`** (new file). `GET /search/person` (searchPerson) + `PUT /people/:id/reassign` (reassignFaces bulk). _Backlog said "global vs space-scoped" but neither endpoint has a space-scoped variant — both are global only. Spec docstring corrects the record._ Pinned: searchPerson auth, empty-name 400, by-name happy path, cross-user isolation (userB doesn't see userA's "Bob"), hidden-by-default exclusion, withHidden=true inclusion. reassignFaces auth, missing destination 400 (requireAccess), cross-user reassign 400, owner-side happy path (face moves Alice → Bob, response contains the updated person).                        | 10 tests | T02        | [x]    |

---

## Phase 6 — Long tail (P3 from research doc)

| ID  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Size     | Blocked by | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ------ |
| T36 | **`queue.e2e-spec.ts`** (new file). Full coverage for the new admin `/queues/*` controller (v2.4.0). Per-verb access matrix (anon 401, non-admin 403). GET /queues lists queues including the fork-bundled `classification`. GET /queues/:name + invalid name 400 (DTO enum). PUT /queues/:name pause/unpause round-trip with try/finally restore. GET /queues/:name/jobs list. DELETE /queues/:name/jobs both with and without `failed: true`.                                                                                                                                                                                                                          | 17 tests | T02        | [x]    |
| T37 | **`notification-admin.e2e-spec.ts`** (new file). Content paths beyond what T31 already covers (T31 had auth + admin gate matrix only). DTO validation: missing title / userId / malformed UUID / invalid level enum / invalid type enum / positive level=Warning + type=SystemMessage round-trip. Template render happy paths for welcome, album-invite, album-update + the empty-fallback default branch for unknown template names. POST /test-email with bogus SMTP host pins the 'Failed to verify SMTP configuration' BadRequestException. _The test-email success path requires a real SMTP server in the e2e stack, out of scope._                                | 12 tests | T02        | [x]    |
| T38 | **`sync.e2e-spec.ts`** (new file). `/sync/full-sync` + `/sync/delta-sync` + `/sync/stream` + `/sync/ack` (GET/POST/DELETE). Per-route auth. Full sync owner happy path + cross-user isolation. Delta sync: very-old cutoff (>100 days) returns needsFullSync=true sentinel, recent cutoff returns the user's recent assets, malformed UUID in userIds 400. Stream returns content-type application/jsonlines+json with parsable jsonl body (custom supertest parser to bypass auto-JSON parsing). Stream invalid SyncRequestType enum 400. Ack list returns empty initially. ArrayMaxSize(1000) on POST /sync/ack. DELETE /sync/ack returns 204 even when no acks exist. | 16 tests | T02        | [x]    |
| T39 | **`auth-extras.e2e-spec.ts`** (new file, sibling to existing web auth spec). change-password (auth, wrong current pwd 400, happy path + re-login). PIN code setup (auth, non-6-digit 400, happy path + status reflects pinCode=true). PIN code change. PIN code reset (with password, status reflects pinCode=false). session unlock + lock round-trip + wrong-PIN 400. /auth/status auth + shape (password=true, pinCode=false for fresh user). _Per-test fresh users via newUser() helper that captures the createUserDto.create-derived `password-${key}` value for password mutations._                                                                              | 12 tests | T02        | [x]    |

---

## Upstream cleanup tasks

These are not part of the Phase 1 backlog but were discovered during it. Each is upstream-rooted and the right fix lives upstream, not in our fork.

- **T-cleanup-01: bring upstream `duplicate.e2e-spec.ts` up to date with the current `/duplicates/resolve` API.** The file lives at `e2e/src/api/specs/duplicate.e2e-spec.ts` (outside our vitest glob), added by upstream PR #25316. Upstream PR #25856 had restructured `e2e/src/api/specs/` → `e2e/src/specs/server/api/` six weeks earlier; PR #25316 didn't notice and put the file in the old path. The file never ran in CI, then a subsequent upstream PR changed the response shape of `POST /duplicates/resolve` (now returns `BulkIdResponseDto[]` instead of `{status, results}`) and the stale spec was left behind. Currently 18 of 21 tests fail. Right fix: PR upstream with the corrected assertions. T01 attempted to move the file into our glob but had to revert because we don't want to maintain a fork-local fix that conflicts every rebase. Estimated effort: 2-4 hours of mechanical assertion rewriting.
- **T-cleanup-02: fix upstream `notification.repository.ts:65-72` GET single bug.** `get(id)` filters with `WHERE deletedAt IS NOT NULL` — should be `IS NULL`. Consequence: `GET /notifications/:id` ALWAYS returns 400 ('Notification not found') for active notifications (because the only rows the query returns are soft-deleted ones, and the service then re-checks and throws). The list endpoint at `search()` correctly uses `IS NULL`, so the practical impact is that no caller can fetch a single notification by id. Discovered during T31 (notification e2e spec); the broken behavior is pinned with a bug-marker test so a future fix will trigger a deliberate test update. Right fix: one-line PR upstream. Trivial.
- **T-cleanup-03: add an embedding-seeding helper to `e2e/src/utils.ts` so smart-search sort logic can be tested end-to-end.** The two-phase CTE sort at `search.repository.ts:370-382` (PR #254) has zero e2e coverage because the e2e Docker stack disables ML and the searchSmart endpoint trips the `isSmartSearchEnabled` gate before reaching the sort logic. The queryAssetId path skips the ML encode call, so seeding embeddings via direct DB INSERT into `smart_search` would unlock real sort coverage. Estimated work: ~1 hour to add `utils.seedSmartSearchEmbedding(assetId, vector)` + a test that asserts asc/desc/relevance ordering. T33 punts on this and pins only the validation surface.

## Decision log

Choices the team made (as opposed to facts observed about the code — see "Observed invariants" below).

- **2026-04-06 — Helper API shape (T02)** — chose `forEachActor(actors, run, expected)` over `it.each` per actor; `run` callback returns supertest `Response` directly. Smoke test at `_helpers.e2e-spec.ts` (underscore prefix groups it at the top of the directory listing). See [T02-helpers-design.md](./2026-04-06-e2e-T02-helpers-design.md).
- **2026-04-06 — `forEachActor` throws Error not `expect` (T02)** — failure messages must include the actor ID. The native `expect(status).toBe(exp)` doesn't surface which actor failed. Custom `Error` does. Affects every downstream spec's debugging UX.
- **2026-04-06 — Minimal `ActorId` (T02)** — ship only the 8 actors that the upfront 3 designs consume. `partner`, `libraryOwner`, `apiKey*`, `sharedLink` get added with their first consumer task. No `utils.ts` re-export.
- **2026-04-06 — Fixture lifetime (T02)** — `buildSpaceContext` returns read-only fixtures bound in `beforeAll`. Tests that need mutation create their own resources with cleanup, OR snapshot+mutate+restore in try/finally.
- **2026-04-06 — Extend `utils.createSpacePerson` rather than create a parallel helper (T02)** — the existing helper at `utils.ts:544` already does most of what we need. T02 extends it (junction insert + return shape + type param) instead of duplicating it under a new name in `actors.ts`. `buildSpaceContext` similarly composes existing `utils.userSetup`/`createSpace`/`addSpaceMember` rather than reinventing.
- **2026-04-06 — Space person ID semantics (T09)** — space `personId` ≠ global `personId`; tests must always derive the path parameter from `createSpacePerson`'s returned `spacePersonId`, never from the global `personId`.
- **2026-04-06 — Smoke test must validate role assignment (T02)** — `/server/ping` and `/users/me` are not enough to prove `buildSpaceContext` assigns roles correctly (both return 200 regardless). Smoke test 3 probes `PATCH /shared-spaces/:id` with `{thumbnailCropY: 0}` (Editor-level — name/description/color/faceRecognitionEnabled/petsEnabled would require Owner per `shared-space.service.ts:197-203`). Editor + Owner pass; Viewer is rejected.

## Observed invariants

Facts about the server code that the tests pin. Distinct from decisions because the team didn't choose them — the existing implementation did. Listed here so future PRs can verify they're still true.

- **`shared_space_person_face` is the load-bearing junction** — `getPersonsBySpaceId` reads `representativeFaceId` directly, but `getPersonAssetIds`, `reassignPersonFaces`, `isPersonFaceAssigned`, `getPersonsForDedup`, the `takenAfter`/`takenBefore` EXISTS subquery, and `faceCount`/`assetCount` denormalization all traverse the junction. T02's helper extension inserts it. Verified at `shared-space.repository.ts:599-1000`.
- **`person.thumbnailPath IS NOT NULL AND != ''` is the listing's hard requirement** — the fork's "minFaces gate" mechanism. In production the dedup job sets it after enough faces; in tests `createSpacePerson` sets it directly. Verified at `shared-space.repository.ts:512-513`. T09 test 11 protects it.
- **400 vs 403 for non-member is endpoint-family-dependent** — timeline-family endpoints route through `requireAccess` and throw `BadRequestException` (400). Shared-space-family endpoints route through `requireMembership` and throw `ForbiddenException` (403). Tests pin to whichever the endpoint actually uses. Verified at `src/utils/access.ts:37-42` and `shared-space.service.ts:1166-1180`.
- **`shared-space-person.dto.ts` query params are `limit`/`offset`/`withHidden`/`named`/`takenAfter`/`takenBefore`** — no `top`, no text-based `name` search. Verified at `shared-space-person.dto.ts:6-40`.
- **`TimeBucketDto` has no `size` parameter** — buckets are always monthly. Verified at `time-bucket.dto.ts:9-144`.
- **`TimeBucketDto` exposes `spacePersonId` and `spacePersonIds` as distinct fields from `personId`/`personIds`** — passing a global `personId` against a space query is the PR #260 bug shape. T06 probes this directly.
- **`PATCH /shared-spaces/:id` has a split role requirement** — metadata fields (`name`, `description`, `color`, `faceRecognitionEnabled`, `petsEnabled`) require Owner; thumbnail / cover-photo fields (`thumbnailAssetId`, `thumbnailCropY`) require only Editor. Verified at `shared-space.service.ts:197-204`. T11 (PUT space-person) inherits the same pattern check; tests must use the right field for the role being probed.
- **Space person `pet` filter applies at BOTH the listing AND the single-person endpoint** — turning `shared_space.petsEnabled=false` not only hides pets from `GET /people` but also rejects `GET /people/:personId` with 400 for any pet personId. The `hidden` filter has no equivalent — hidden persons remain addressable via direct fetch. Verified at `shared-space.service.ts:633-636` — the service explicitly checks `!space?.petsEnabled && person.type === 'pet'` and throws `BadRequestException('Person not found')`. UX consequence: turning pets off makes the entire pet sub-graph invisible to all members. Pinned by T10.
- **Space-person sub-endpoints return 400 with message "Person not found" for unknown OR cross-space personIds** — `getSpacePerson` (and PUT/DELETE/merge) call `requireMembership` for the _space_ check (which throws `ForbiddenException` for non-members), then run `getPersonById` and manually `throw new BadRequestException('Person not found')` if the person row is missing OR `person.spaceId !== spaceId`. Verified at `shared-space.service.ts:625-636, 667-669` and similar in PUT/DELETE/merge handlers. Tests should pin 400 (not 404) for unknown personIds — the manual throw uses BadRequestException, not NotFoundException, even though the path looks "404-shaped" semantically. T11+ inherit this convention.
- **Timeline `spaceId` query does NOT filter library assets by `isOffline`** — `asset.repository.ts:835-849` joins `shared_space_library` on `(libraryId, spaceId)` but lacks an `asset.isOffline = false` predicate. Consequence: a library asset whose underlying file went offline is still listed in the space's timeline bucket (even though `access.repository.checkSpaceAccess` does filter on `isOffline=false` in a different code path used elsewhere). The thumbnail/download endpoints will probably 404 on the missing file, but the asset is visible. Not necessarily a bug — could be a deliberate UX choice — but worth pinning. T17 test 5 asserts the actual visible-when-offline behavior so a future change requires deliberate update.
- **`DELETE /libraries/:id` is a soft-delete + async cascade** — `library.service.ts:370-379` calls `softDelete(id)` (sets `deletedAt`) and queues a `LibraryDelete` job, then returns 204 immediately. The `handleDeleteLibrary` job processes asynchronously and eventually hard-deletes the library row, which triggers the FK cascade on `shared_space_library` (FK is `onDelete: CASCADE`). Tests that need to assert the cascade must `await utils.waitForQueueFinish(admin.accessToken, 'library')` after the DELETE call. Pinned by T17 test 6.
- **`PUT /shared-spaces/:id/libraries` has a TWO-step gate** — admin gate FIRST (`if (!auth.user.isAdmin) → ForbiddenException('Only admins can link libraries to spaces')`, line 450-452), then `requireRole(Editor)` for the space membership check (line 454). The two-step ordering means a non-admin owner of the space gets 403 from the admin gate, not from the role check. `addLibrary` returns null for duplicate (spaceId, libraryId) without error, so the HTTP response is 204 either way — idempotent, not 409. Verified at `shared-space.service.ts:449-477`. Pinned by T15.
- **`POST /shared-spaces/:id/people/deduplicate` is Owner-only and uses jobId-based queue dedup** — verified at `shared-space.service.ts:721-728`. The dedup endpoint requires `requireRole(Owner)`, distinct from PUT/DELETE/merge which only need Editor. The queued job uses jobId `space-dedup-${spaceId}` (`job.repository.ts:239-241`), so calling deduplicate twice in quick succession enqueues exactly one BullMQ job. PR #292 invariant. T14 pins via pause-and-verify on the FacialRecognition queue (admin token required for queue inspection).
- **Space person aliases are PER-USER and `requireMembership`-only** — the alias is stored as `(personId, userId, alias)` in a separate table. `getAlias(personId, auth.user.id)` returns only the caller's row. Owner setting "Mom" as Alice's alias is invisible to editor and viewer. Both PUT and DELETE require only `requireMembership` (NOT Editor role) — aliases are personal metadata, not space state, so a read-only viewer can label people for themselves. DELETE has no person existence check and is idempotent on missing personId; PUT validates and returns 400. Verified at `shared-space.service.ts:780-803`. Pinned by T13.
- **`getSpacePersonThumbnail` has THREE return paths once the access check passes** — at `shared-space.service.ts:643-657`: (a) person not found / wrong space → `NotFoundException` → 404; (b) `thumbnailPath` is null/empty → `NotFoundException` → 404; (c) `thumbnailPath` set → `serveFromBackend(...)` which streams the file (200) or fails (500) if the file doesn't exist on disk. T10 pins the 404-graceful path by transiently blanking `person.thumbnailPath` and asserting `{member: 404, non-member: 403, anon: 401}` — the layered 401 < 403 < 404 ordering. The 200 path is not exercised because `utils.createSpacePerson` uses a fictional fixture path; testing the 200 path would require a real file in the upload location and is deferred to a follow-up.
- **`partner.inTimeline` defaults to `false`** — verified at `server/src/schema/tables/partner.table.ts:46`. Newly-created partnerships are invisible to `getMyPartnerIds({timelineEnabled: true})` until the recipient calls `PUT /partners/:id` with `{inTimeline: true}`. The `addPartner` helper in `e2e/src/actors.ts` auto-enables this so test usage is intuitive — but it's an easy footgun for any future spec that creates partnerships outside the helper. Pinned by T04's withPartners tests.
- **`withSharedSpaces` and `withPartners` reject undefined visibility** — `timeline.service.ts:91-113` treats `visibility === undefined` as `requestedArchived = true` and throws 400. Tests using these flags MUST pass `visibility=timeline` explicitly. Pinned by T04 tests.
- **Default visibility filter is permissive (Timeline + Archive)** — `withDefaultVisibility` at `server/src/utils/database.ts:79-81` is `where('asset.visibility', 'in', [Archive, Timeline])`, NOT just `Timeline`. The web UI's "main timeline" view passes `visibility=timeline` explicitly to exclude archive. Tests querying without an explicit visibility include archived assets in the count. Pinned by T05 tests.
- **`POST /faces` returns void, not the face row** — `person.service.ts:639` declares `Promise<void>`, so callers cannot read back the created face id from the HTTP response. Tests that need the id (T07 PUT/DELETE) use `utils.createFace` which inserts via direct DB and returns the id explicitly. Pinned by T07.
- **`PUT /faces/:id` semantics is "path = target person, body = face"** — `face.controller.ts:45-58` and `person.service.ts:111-126` confirm: the URL path :id is the _destination person ID_ (where to move the face TO), and the body's `{id}` is the _face ID_ (which face to move). The shared `FaceDto` is reused across endpoints with different meanings — confusing but stable. Pinned by T07.
- **Stable space-people sort across paginated calls** — T09's test 10 pins it. T10–T12 inherit the assumption.

## Known footguns

- **`utils.createSpacePerson` uses a fictional fixture thumbnailPath** — `'/my/awesome/thumbnail.jpg'` is set to satisfy the listing's hard requirement that the underlying global person have a non-empty thumbnailPath. Since the file doesn't exist on disk, any test that wants to exercise the **200 path** of `GET /people/:personId/thumbnail` would trip `serveFromBackend` and get a 500. T10 sidesteps this by blanking the path transiently and pinning the **404 graceful path** instead. A follow-up task should make `createSpacePerson` accept a real fixture file in the upload location so the 200 path can be tested directly. Not a server bug — the service has graceful 404 handling at `shared-space.service.ts:652-654`; the 500 only happens when the path is non-empty AND the file is missing, which is a fixture setup question.

## Known flaky-spec footgun

- **`utils.createPerson` + `Promise.all` is unsafe with the shared `pg.Client`** — `createPerson` calls `setPersonThumbnail` which uses the module-level `client`. T07/T08/T09 do `Promise.all([createPerson, createPerson, createPerson])` for parallel user setup, which can interleave queries on the same connection. pg currently warns (`DeprecationWarning: Calling client.query() when the client is already executing a query`) and will throw in pg@9.0. Observed as a one-shot FK violation in T09 setup that didn't reproduce on subsequent runs. Fix later by serialising the parallel createPerson calls in test setup, or migrating utils to a connection pool. Not blocking — current retry rate ~0.

## Open hypotheses

Things the upfront designs claim about server behaviour but cannot verify themselves. The first task that can verify resolves them.

_(none currently open)_

## Resolved hypotheses

- **`hidden` filter is listing-only, `pet` filter is BOTH listing and single-fetch** — RESOLVED by T10. Hypothesis was half right: hidden persons are addressable directly (200), but pets-when-`petsEnabled=false` are NOT addressable (400). The pet filter applies at the single-person endpoint too, which is an asymmetry vs the hidden filter. UX consequence: turning off pets in a space hides the entire pet sub-graph, even if a member knows the pet's ID. Pinned by T10 tests.

---

## Out of scope

- **Mobile / Flutter API tests.** Mobile uses the same controllers but exercises them through generated Dart clients. The API e2e suite covers the contract; mobile-specific behaviour is its own pyramid.
- **Component tests in `web/`.** Some of the gaps could be covered by Vitest component tests with mocked SDKs. The trade-off (memory: `feedback_e2e_mock_filterpanel`) is unreliability for FilterPanel-shaped components. Default to API e2e for access-control work; component tests stay reserved for pure rendering logic.
- **Retrofitting helpers into existing specs.** Existing specs work fine. They get the helper treatment opportunistically when touched for other reasons.
- **CI gate enforcing "every new endpoint has an e2e test".** Worth considering after the backlog drains, not before.
